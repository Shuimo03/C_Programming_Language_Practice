# C语言笔记-程序设计入门—C语言

## week1-2

### 计算机语言

1. 程序是用特殊的编程语言写出来表达如何解决问题的
2. 不是用编程语言来和计算机交谈，而是描述要求它如何做事情的过程或方法

### 算法

- 我们让计算机做计算，就需要像这样超出计算的步骤，然后用编程语言实现。
- 计算机做的所有事情被称为计算。
- 计算的步骤就是算法。

### 计算机思维

枚举

二分法

### 程序的执行

- 解释：借助一个程序，那个程序能试图理解你的程序，然后按照你的要求执行。
- 编译：借助一个程序，就像一个翻译，把你的程序翻译成计算机真正能懂的语言，机器语言，然后这个机器语言写的程序就能直接执行了。

任何一种语言都可以进行解释或编译，解释类语言有特殊的计算能力，编译语言有确定的运算性能。

### C的优点

### C的历史

### C的发展与版本-标准

- 1989年ANSI发布了一个标准-ANSI C
- 1990年ISO接受了ANSI的标准-C89
- C的标准在1995年和1999年两次更新C95和C99

## 第二周：计量

### 变量

```c
 int price = 0; // 定义了一个变量，变量的名字是price，类型是int，初始值是0
```

变量是一个保存数据的地方，当我们需要在程序里保存数据的时候，就需要一个变量。

### 变量定义

- 变量的定义一般形式如下
  - 类型名称
  - 变量名称
- int price;
- int amount;
- int price,amount;

### 变量的名字

- 变量需要一个名字，变量的名字是一个标识符，意思是它用来识别这个和那个不同的名字。
- 标识符有标识符的构造规则。基本原则是：标识符只能由字母、数字和下划线组成，数字不可以出现在第一个位置上，C语言的关键字或保留字不可以用来做标识符。

### 赋值和初始化

- 和数学不同，a = b在数学中表示关系，而在程序设计中， a = b表示动作，关系是静态的，动作是动态的，在数学中，a = b和b = a完全相同，但是在程序设计中就不可以，两者意思完全相反。
- 当赋值发生在定义变量的时候，就是变量的初始化，虽然C语言并没有强制要求所有的变量都在定义的地方做初始化，但是所有的变量在第一次被使用(出现在赋值运算符右边)之前就应该被赋值一次。

### 变量类型

C语言是一种有类型的语言，所有的变量在使用之前必须定义或声明，所有的变量必须具有确定的数据类型。数据类型表示在变量中考研存放什么类型的数据，变量中只能存放指定类型的数据，程序运行过程中也不能改变变量的类型。

### 数据类型

### 数据运算

- 两个整数的运算结果只能是整数。
- 整数相除，会抛弃小数点。
- 当浮点数和整数放在一起运算的时候，C会将整数换算成浮点数，然后进行浮点数的运算。

### 浮点数

带小数点的数值，浮点这个词的本意就是指小数点是浮动的，是计算机内部表达非整数(包含分数和无理数)的一种方式，另一种方式叫做定点数，不过在C语言中碰不到定点数，定点数是指小数点固定的数字。

### scanf和prinf

出现在scanf里面的东西一定是要输入的东西，

### 表达式

一个表达式是一系列运算符和算子的组合，用来计算一个值。

- 运算符：是指进行运算的动作，比如加法运算符"+"，减法运算符"-"等等。
- 算子：是指参与运算的值，可能是常量，也有可能是变量，还有可能是一个方法的返回值。

### 运算符优先级

### 结合关系

- 一般自左向右g'g
- 单目+-和赋值=自右向左

### 复合运算

### 递增和递减运算符

"++"和"--"是两个很特殊的运算符，它们是单目运算符，这个算子还必须是变量，这两个运算符分别叫做递增运算符和递减运算符，只能用于变量+1或-1。

"++"和"--"可以放在变量的前面。叫做前缀形式，也可以放在变量的后面，叫做后缀形式，a++是+1之前的值，++a是加了1之后的值，无论那个，a本身都加了1。

### 常量

固定不变的数，被称为是常数，直接写在程序里面的，被称为直接量。

### 如何输入

### 如何输出

## week3-5

### 判断

- if语句
- if else语句
- if else if语句

C语言提供了六个关系运算符，计算两个值之间的关系，所以叫做关系运算。

- == 相等
- != 不相等
- ">" 大于
- < 小于
- ">"= 大于等于
- "<=" 小于等于

else和最近的if匹配，括号情况除外。

### 关系运算

关系运算只有2个值，0(false)或1(true)，关系运算符的优先级要比算术运算的低，但是比赋值高，判断是否相等的==和!=的优先级比其他的低，而连续的关系运算是从左到右计算。

### 注释

以两个斜杠"//"开头的语句把程序分开，注释插入在代码程序中，用来向读者提供解释信息，它们对于程序的功能没有任何影响，但是往往能使得程序更加容易被人类读者理解。

/**/也是注释，这个是多行注释，单行注释只有C99有

### 循环

- while循环
- do-while循环
- for循环

### while循环

- 把while循环翻译成当，那么一个while循环的意思就是：当条件满足的时候，不断重复循环体内的语句。
- 循环执行之前判断是否继续循环，所以有可能循环一次也没有被执行；
- 条件成立是循环继续的条件。

### do-while循环

在进入循环的时候不做检查，而是在执行完一轮循环体的代码之后，再来检查循环的条件是否满足，如果满足则继续下一轮循环，不满足则结束循环，至少执行一次。

do-while和while的区别在于，do-while的循环体执行结束的时候才判断条件，与while相同的时候在于，条件满足的时候执行循环，不满足的时候结束循环。

### for循环

for循环像一个计数循环，设定一个计数器，初始化它，然后计数器到达某值之前，重复执行循环体，而每执行一轮循环，计数器值以一定步进进行调整，比如加1或减1。

### 循环的选择

for循环可以和while互相改写，改写如下

```
for(初始动作;条件;每轮的动作){} = for(;条件;) == while(条件)
```

- 如果有固定次数，用for循环
- 如果必须执行一次，用do_while循环
- 其他情况用while循环

### 循环控制

- break:跳出循环
- continue:跳过循环这一轮剩下的语句进入下一轮

### debug技巧

- 利用好printf
- 

## week4-6

### 逻辑类型与运算

使用Boolean类型需要导入头文件stdbool.h，但是里面仍然是整数类型，true = 1，false  = 0；

### 逻辑运算

- 逻辑运算是对逻辑量进行的运算，结果只有0或1
- 逻辑量是关系运算符或逻辑运算的结果

| 运算符 | 描述   | 示例     | 结果                                                   |
| ------ | ------ | -------- | ------------------------------------------------------ |
| !      | 逻辑非 | !a       | 如果a是0结果就是1，a是1结果就是0                       |
| &&     | 逻辑或 | a && b   | 如果ab都是1，结果为1，其中有一个不是1，结果就是0       |
| \|\|   | 逻辑或 | a \|\| b | 只要ab其中一个为1，结果就是1，只有两个都为0，结果才为0 |

### 优先级(从高到低)

![image-20200603141017935](C:\Users\wu-cola\AppData\Roaming\Typora\typora-user-images\image-20200603141017935.png)

### 短路机制

- 逻辑运算是自左向右进行，如果左边的结果可以决定结果，就不会执行右边计算。
- 对于&&来说，左边是false的时候就不做右边了。
- 对于||来说，左边是true的时候就不做右边了。

### 条件运算符

条件?条件满足的值:条件不满足的值，使用如下，条件运算符是自右向左结合的。

```c
#include<stdio.h>

int main(){

    int a = 10;
    int b = 20;
    int res = (a > b) ? 1:0;
    //相当下面这个if语句
    if(a > b){
        res = 1;
    }
    else{
        res = 0;
    }
    printf("%d\n", res);
    return 0;
}
```

条件运算符的优先级高于赋值运算符，但是低于其他运算符。

### 逗号运算符

逗号用来连接两个表达式，并以其右边的表达式的值作为它的结果。逗号的优先级是所有的运算符中最低的，所以它两边的表达式会先计算；逗号的组合关系是自左向右，所以左边的表达式会先计算，而右边的表达式的值就留下作为逗号运算的结果，主要用在for循环。

### 级联判断 if else if

### 多路分支 switch-case

```c
switch(控制表达式){ 控制表达式只能是整数结果
	case 常量: 常量可以是常数，也可以是常数表达式
	语句
	....
	case 常量:
	语句
	....
    default:
    语句
    ....
}

#include<stdio.h>

int main(){

    int type = 0;
    scanf("%d", &type);

    switch(type){

        case 1:
            printf("你好");
            break;
        case 2:
            printf("早上好");
            break;
        case 3:
            printf("下午好");
            break;
        case 4:
            printf("再见");
            break;
        default:
            printf("啊?什么啊?");
    }
    return 0;
}
```

switch语句可以看作是一种基于计算的跳转，计算控制表达式的值后，程序会跳到相匹配的case处，分支标号只是说明了switch内部位置的路标，在执行完分支中最后一条语句后，如果没有后面的break，就会顺序执行到下面的case里面去，知道遇到下一个break或者switch结束为止。

### switch-case和if else if区别

### 数组

数组是一个相同数据类型的集合，数组的长度是固定的，数组的特点如下

- 所有元素具有相同的数据类型
- 一旦创建，不能改变大小
- 数组中的元素在内存中是连续依次排列的
- 数组的每个单元就是数组类型的一个变量
- 使用数组的时候放在[]中的数字叫做下标或索引，下标从0开始数。
- [0,数组大小-1]

### 数组定义

- <类型>变量名称[元素数量];
  - int grades[100]
- 元素数量必须是整数
- C99之前元素数量必须是编译时确定的字面量

### 函数

函数是一块代码，接受零个或多个参数，做一件事情，并且返回零个或一个值。

```c
int count(int n){ //函数头 = 返回类型 函数名 参数表
    int res = 0;
    for (int i = 1; i <= n; i++){ //{}内是函数体
        res += i;
    }
    return res;
}
```

调用函数是函数名(参数值)

- ()起到了表示函数调用重要性
- 即使没有参数也需要()
- 如果有参数，则需要给出正确的数量和顺序
- 这些值会按照顺序依次用来初始化函数中的参数
- 函数知道每一次是哪里调用它，会回到正确的地方。

### 函数调用过程

### 函数先后关系

C的编译器是自上而下的，如果顺序不对，编译器会根据函数参数表去猜函数类型，如果想让函数在main函数下，就需要对函数进行声明，被声明的函数也被称为函数原型。函数原型作用是告诉编译器这个函数长什么样子，函数声明的参数列表，可以没有变量名。

- 函数名
- 函数参数列表
- 返回类型

### 调用函数

- 如果函数有参数，调用函数的时候必须给它数量、类型正确的值。
- 可以传递给函数的值是表达式结果，如下
  - 字面量
  - 变量
  - 函数的返回值
  - 计算的结果

C语言调用函数的时候，只能传递传值给函数，每个函数有自己的变量空间，参数也位于这个独立的空间中，和其他函数没有关系。

### 本地变量

函数每次运行，就会产生一个独立的变量空间，在这个空间中的变量，是函数的这次运行独有的，称作本地变量。

- 定义在函数内部的变量就是本地变量
- 参数也是本地变量

### 变量的生存期和作用域

- 生存期：什么时候该变量出现了，到什么时候该变量消失了。
- 作用域：代码什么范围内可以访问这个变量

对于本地变量来说，以上两个问题都是在大括号内的，被称为块。

### 本地变量规则

- 本地变量是定义在块内的
- 可以定义在函数的块内
- 可以定义在语句的块内
- 甚至可以随便拉一对大括号来定义变量
- 程序运行进入到这个块的时候，其中变量不存在，离开这个块的时候，其中变量消失了。
- 块外面定义的变量在里面仍然有效
- 块外和块内的变量名相同，块内的变量会覆盖块外的。
- 不能在一个块内定义同名的变量
- 本地变量不会被默认初始化

C语言不允许函数嵌套定义

### 程序运行和main函数

### 二维数组

```
int arr[row][col]
```

- 列数必须给出，行数可以由编译器来数
- 每行一个{},逗号分隔
- 最后的逗号可以存在
- 如果省略，自动填充0

## week7-8

### 集成初始化定位

- 用[n]在初始化数据中给出定位
- 没有定位的数据在前面的位置后面
- 其他位置为0
- 也可以不给出数组大小，让编译器计算
- 适合初始数据稀疏的数组。

```c
int a[10] = {
	[0] = 2,
	[2] = 3,6,
};
```

### 数组大小

- sizeof给出整改数组所占的内容大小，单位是字节
- sizeof(a) / sizeof(a[0]) a为数组，a[0]为数组第一个元素。

### 数组赋值

- 数组本身不能被赋值
- 要把一个数组的所有元素交给另一个数组，必须采用遍历

### 指针

- 指针是保存地址的变量。
- sizeof是一个运算符，给出某个类型的变量在内存中所占据的字节数。
- &符号获得变量地址，操作数必须是变量。
- 地址的大小和取决于int的编译器有关。
- 变量的值是内存地址
- 普通变量的值是实际的值
- 指针变量的值是具有实际值的变量的地址
- 指针可以作为参数
- 访问地址上的变量使用*，这是一个单目运算符，用来访问指针的值所表示的地址上的变量。

### 内存模型

### 指针和数组

- 函数参表中的数组实际上是指针
- sizeof(a) == sizeof(int*)
- 但是可以用数组的运算符[]进行运算
- 数组变量本身表达地址，所以int a[10]; int *p = a这样子的就不需要&取地址
- 但是数组的单元表达是变量，需要用&取地址
- a == & a[0]
- []运算符可以对数组做，也可以对指针做
- 数值变量是const指针，所以不能赋值
- 数组变量是一个特殊的指针

### 字符类型

- char是一个整数，也是一个特殊的类型：字符，这是因为
  - 用单引号表示的字符字面量 '1','a'
  - "也是一个字符
  - printf和scanf里面用%c来输入输出字符

计算机内部使用的是ASCIl编码

### 字符计算

- 一个字符加一个数字得到ASCII码表中那个数之后的字符。
- 两个字符的减，得到它们在表中的距离

在ASCII码中数字0到9是依次排列的，字母是顺序排列，大小写字母分开排列。

### 字符串

C语言的字符串是以字符数组的形态存在的。

- 以0结尾的一串字符
- 0或'\0'是一样的，但是和'0'不同
- 0标志字符串的结束，但是它不是字符串的一部分
- 计算机字符串长度的时候不包括这个0
- 字符串以数组的形式存在，以数组或指针的形式访问
- 更多的是以指针的形式
- string.h里面由很处理多字符串的函数
- 不能使用运算符对字符串运算
- 通过数组的方式可以遍历字符串
- 唯一特殊的地方是以字符串字面量可以用来初始化字符数组

### 字符串变量

- char *str = "Hello";
- char word[] = "Hello"
- char line[10] = "Hello";

两个相邻的字符串，如果中间没有任何符号，会连接在一起。

### 字符串使用方式

- 数组：这个字符串在这里，作为本地变量空间自动被回收。
- 指针：这个字符串不晓得在哪里，处理参数，动态分配空间。

如果要构造一个字符串就是用数组，如果要处理一个字符串就使用指针。

### string.h常用函数

- strlen 返回字符串长度
- strcmp 比较两个字符串
  - 如果等于0说明两个字符串相等
  - 如果等1说明A字符串要大于B字符串
  - 如果等-1说明B字符串要小于A字符串
- strcpy 把第二个字符串内容复制到第一个字符串
- strcat 把第二个字符串接到第一个字符串后面组成一个新字符串。
- strhr
- strstr

